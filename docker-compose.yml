services:
  bot:
    build: .
    env_file:
      - .env
    restart: unless-stopped
    command: ["node", "src/index.js"]

  deploy:
    profiles: ["ops"]
    build: .
    env_file:
      - .env
    command: ["node", "src/deploy-commands.js"]

